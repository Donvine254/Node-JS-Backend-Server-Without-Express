{
  "name": "projects",
  "version": "1.0.0",
  "description": "sample node backend",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js",
    "build": "node ./app/sync && node ./app/seed"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^8.0.2"
  },
  "dependencies": {
    "sequelize": "^6.32.1",
    "sqlite3": "^5.1.6"
  }
}
